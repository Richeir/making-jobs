/**
 * Single source of truth for the deployment location.
 * Imported by config.mts (vitepress), tools/build-checklist.mjs (Node),
 * and any theme component that needs to emit an absolute site link.
 * Keep in sync with the GitHub Pages URL — this is the only copy.
 */
export const SITE = "https://richeir.github.io";
export const BASE = "/making-jobs/";
